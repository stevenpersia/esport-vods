<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mt-4">
  <%= form.text_field :name, {:class => 'form-control form-control-lg', :placeholder => "Nom de l'évènement", :required => true }  %>
  </div>

  <div class="input-group mb-4">
  <div class="input-group-prepend">
    <label class="input-group-text">Slug</label>
  </div>
  <%= form.text_field :slug, {:class => 'form-control form-control-lg', :placeholder => "slug", :required => true } %>
  <small class="form-text text-muted">
  Ne pas insérer d'accents, de caractères spéciaux et d'espaces.
  </small>
  </div>

  <%= form.submit 'Enregistrer', :class => 'btn btn-primary hand btn-lg btn-block mb-3' %>
<% end %>
